import { DirtyCheckDirective } from './dirty-check.directive';

describe('DirtyCheckDirective', () => {
  it('should create an instance', () => {
    const directive = new DirtyCheckDirective();
    expect(directive).toBeTruthy();
  });
});
